/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    // your images configuration goes here
  },
  // output: "standalone",
};

module.exports = nextConfig;
